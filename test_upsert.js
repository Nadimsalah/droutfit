const { createClient } = require("@supabase/supabase-js"); require("dotenv").config({ path: ".env.local" }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function test() { const { error } = await supabase.from("profiles").upsert({ id: "00000000-0000-0000-0000-000000000000", shopify_domain: "test.myshopify.com" }); if (error) console.error("Error:", error.message); else console.log("Success!"); } test();