const { createClient } = require("@supabase/supabase-js"); require("dotenv").config({ path: ".env.local" }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function update() { const { error } = await supabase.rpc("add_column", { table_name: "profiles", column_name: "shopify_domain", column_type: "text" }); if (error) { console.error("RPC failed, trying SQL execution if possible:", error); const { error: sqlError } = await supabase.from("profiles").select("id").limit(1); if (sqlError) console.error("SQL check failed:", sqlError); } else { console.log("Column added successfully via RPC"); } } update();